if ($('#brand_products').length > 0) {
    $('#brand_products').html("<%= escape_javascript(render("products/page")) %>");
} else {
    $('#content').html("<%= escape_javascript(render("products/page")) %>");
}