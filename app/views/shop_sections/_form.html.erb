<%= form_for(@shop_section) do |f| %>
    <% if @shop_section.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@shop_section.errors.count, "error") %> prohibited this shop_section from being saved:</h2>

          <ul>
            <% @shop_section.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="field">
      <%= f.label :name %>
      <br/>
      <%= f.text_field :name %>
    </div>
    <div class="field">
      <%= f.label :short_url %>
      <br/>
      <%= f.text_field :short_url %>
    </div>
    <div class="field">
      <%= f.label :list_order %>
      <br/>
      <%= f.number_field :list_order %>
    </div>
    <div class="field">
      <%= f.label :description %>
      <br/>
      <%= f.text_area :description %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>
