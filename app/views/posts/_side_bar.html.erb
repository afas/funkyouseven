<div class="sidebar">
  <div class="title_sidebar">
    Читайте также
  </div>

  <% @posts_side_bar.each do |post| %>
      <%= render "posts/post_preview_item", :target => post, :second => false, :show_date => false, :no_margin => false %>
  <% end %>

  <div class="block_second">
  </div>

  <div class="title_sidebar">
    Новое в магазине
  </div>

  <% @shop_new_sidebar.each do |product| %>
      <%= render "products/product_preview_item", :product => product, :cat_item => false, :margin_top => false, :container_class => false %>
  <% end %>
</div>