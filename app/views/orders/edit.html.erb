<%= gmaps({
                  :map_options => {
                          :auto_zoom => false,
                          :zoom => 13,
                          :do_clustering => true
                  },
                  :markers => {
                          :data => @map_point
                  }
          })
%>

<div class="center_item">
  <div class="main noborder">
    <h1><%= t('activerecord.attributes.order.title') %></h1>
    <%= render 'form' %>
  </div>

  <%#= render "service_partial/social_panel", :target => @post %>
</div>

<%#= render "posts/side_bar" %>
